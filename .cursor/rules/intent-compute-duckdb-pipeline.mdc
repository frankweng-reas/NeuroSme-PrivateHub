---
description: Intent v4 JSON → compute_engine → DuckDB SQL → chart_result → LLM 文字；schema 來自 bi_schemas（含 UI 建立）；非 v4 拒絕
globs: backend/app/services/compute_engine*.py,backend/app/services/duckdb_store.py,backend/app/services/schema_loader.py,backend/app/api/endpoints/chat_compute_tool.py,backend/app/schemas/intent_v4.py,docs/intent_v4_protocol.md,config/system_prompt_analysis_text_tool.md,config/system_prompt_analysis_intent_tool.md,frontend/src/components/SchemaManagerOverlayV2.tsx
alwaysApply: false
---

# Intent → 計算 → DuckDB → 輸出（NeuroSme）

## 端到端流程

1. **結構化意圖**：LLM / 請求帶入 **Intent v4.0** JSON（須含 `"version": "4.0"`）；shape 與欄位見 `docs/intent_v4_protocol.md`。自然語言提示詞：`config/system_prompt_analysis_intent_tool.md`（純規則，**無占位符**）；schema 與層級由 `_build_user_content_intent()` 注入 **user message 最前段**（非 system prompt）。
2. **Schema**：由 PostgreSQL **`bi_schemas.schema_json`** 載入 **`schema_def`**（`load_schema_from_db` / `_resolve_schema_def`）；缺少則無法計算。正式路徑不依賴 repo 內 YAML。
3. **產品側建立／編輯 Schema**：`frontend/src/components/SchemaManagerOverlayV2.tsx` — Wizard 建立／更新 `bi_schemas`；CSV 可自動帶入 **`col_1`, `col_2`, …** 與別名；**`dimension_hierarchy`** 寫入 `schema_json`，由 `chat_compute_tool` 轉成區塊注入 intent user message。dim 欄位可設定 **`enum_values`**（可選值清單），供 UI 顯示與 prompt 注入。存檔後父層應 **`onSchemaChanged`** 刷新可選 schema。
4. **計算入口**：多數專案流程走 `chat_compute_tool` 的 `_compute_with_intent` → **`run_compute_engine(duckdb_name, intent, schema_def)`**，其中 `duckdb_name` 通常等於 **project_id** 字串。
5. **引擎核心**：`compute_engine.run_compute_engine` 驗證 v4 → `resolve_duckdb_path` → **`run_sql_compute_engine_v4`**（`compute_engine_sql.py`）：`IntentV4` 驗證 → **`try_build_sql_v4`**（`compute_engine_sql_v4.py`）→ SQL **僅查單表 `data`** → **`execute_sql_on_duckdb_file`**（`duckdb_store.py`）→ DataFrame → `chart_from_sql_dataframe` / list 變體 → **`chart_result`**。欄位須在 **`schema_def.columns`** 的 key 白名單內（`column_allowlist_from_schema`）。
6. **文字階段**（例如 `compute-from-intent`）：先有 `chart_result`，再 `_chart_result_to_detail_lines` 組區塊，用 **`_load_prompt("text")`**（預設可對應 `config/system_prompt_analysis_text_tool.md`）呼叫 LLM 產 **Markdown 分析文字**；數字須與計算結果一致。

## enum_values 自動偵測

CSV 匯入成功後，`bi_projects.py` 的 `_detect_and_patch_enum_values()` 會掃描所有 dim 欄位（非 dim_time）：
- 若 distinct 值 ≤ 10（`_ENUM_VALUES_MAX`）→ 寫入 `bi_schemas.schema_json` 的 `enum_values`（union 合併，不覆蓋舊值）
- 若 distinct 值 > 10 → 標記 `enum_values: []`（高基數，不注入 prompt）

`_build_schema_block` 讀取 `enum_values` 後在 user message schema 區塊中附上 `（可選值：A, B, C）`，提升 LLM 篩選條件的準確度。

## Prompt 組裝架構

```
system prompt  = config/system_prompt_analysis_intent_tool.md（純規則，不含 schema）
user message   = # Data Schema\n{schema_block}\n\n
                 **層級** {hierarchy_block}\n\n
                 **輸出的每個 col_N 必須出現在上方 Data Schema 的 columns 清單中**\n\n
                 當前時間：{now_str}\n\n
                 問題: {question}
```



## 換產業／多 Schema

- **可運作條件**：該專案綁定的 **`schema_id`** 與 **DuckDB `data` 表欄位** 必須與同一份 **`schema_json.columns`** 的 key 一致（CSV `import-csv` 依 schema 做 mapping）。只改 `bi_schemas` 而不依新 schema 重匯資料會失敗或結果錯誤。
- **模型對欄**：Intent 內僅能使用真實存在的欄位代碼；Overlay 預設 **`col_N`** 與 v4／prompt 契約一致。**時間篩選**行為依欄位 **`attr: dim_time`**（見 `compute_engine_sql_v4`）與型別設定；UI 上 CSV 推斷型別有領域偏置，新產業宜手動核對。
- **引擎限制**：v4 產生器不做 **多表 JOIN**；星狀／多來源分析需先 **ETL 成單一寬表** 再進 DuckDB。

## 行為約束

- **非 v4 intent**：`run_compute_engine` 直接錯誤，不做舊版 fallback。
- **無效 v4**：Pydantic 驗證失敗時對外訊息以 `USER_FACING_INTENT_V4_VALIDATION_MESSAGE` 等為準。
- **除錯**：`debug` / 產生 SQL 是否回傳依 **`settings.EXPOSE_COMPUTE_ERROR_DETAIL`**。

## 主要 API 線索（同檔多條路由）

- `POST .../compute-engine`：`duckdb_name` + `schema_id` + intent → `run_compute_engine`。
- `compute-from-intent` / streaming：`project_id` 對應 DuckDB → `_compute_with_intent` → 成功後再走文字 LLM。

修改管線時優先讀：`chat_compute_tool.py`（流程與 LLM）、`compute_engine.py`、`compute_engine_sql.py`、`compute_engine_sql_v4.py`、`duckdb_store.py`、`schema_loader.py`、`schemas/intent_v4.py`。產品 Schema 編輯與匯入對齊：`SchemaManagerOverlayV2.tsx`、`bi_projects.py`（`import-csv`）。
