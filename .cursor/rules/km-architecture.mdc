---
description: KM 知識庫架構設計決策與 ownership 規則
globs: backend/app/models/km_*.py,backend/app/api/endpoints/km*.py,backend/app/services/km_service.py,frontend/src/api/km.ts,frontend/src/pages/agents/AgentKbManager*.tsx
alwaysApply: false
---

# KM 知識庫架構設計

## Ownership 模型

文件（KmDocument）一定歸屬某個知識庫（KmKnowledgeBase）。  
權限與可見性判斷**必須走 KB 層級**，不可使用 `KmDocument.owner_user_id`。

```
KmKnowledgeBase
  ├── created_by  → 建立 KB 的 user id
  ├── scope       → "personal" | "publish"
  └── KmDocument（多個）
        └── KmChunk（多個）
```

## Scope 語意

| KB scope | 說明 | 可部署 Bot |
|----------|------|-----------|
| `personal` | 僅建立者可見，草稿與個人測試用 | ❌ |
| `publish`  | 建立者準備對外服務的知識庫，透過 Bot Builder 部署 Bot | ✅ |

KB Manager 後台一律只有 KB 建立者（+ admin）可見，無論 scope 為何。
員工查詢統一透過 Bot Widget，不在後台直接搜尋。

## 權限規則

| KB scope | 可管理（編輯/上傳/刪除）的使用者 |
|----------|---------------------------------|
| `personal` | `kb.created_by == current.id` 或 `current.role in ("admin", "super_admin")` |
| `publish`  | `kb.created_by == current.id` 或 `current.role in ("admin", "super_admin")` |

建立/變更 scope 為 `publish` 需要 `current.role in ("admin", "super_admin", "manager")`。

## KmDocument.owner_user_id 是 LEGACY 欄位

- 這是早期文件系統沒有 KB 概念時遺留的欄位
- `knowledge_base_id = NULL` 的極少數舊文件才用到它
- **新功能不得使用此欄位判斷權限**
- `km_service.py` 的 RAG 篩選邏輯有保留 legacy 相容路徑，但新 endpoint 不應仿效

## KB Scope vs Document Scope 對應

所有 KB 的文件 scope 一律為 `private`：

| KB scope | Document scope |
|----------|---------------|
| `publish`  | `private` |
| `personal` | `private` |

`KmDocument.scope` 欄位本身是 legacy 概念的殘留，現行邏輯以 KB scope 為主。
Bot Widget 的 RAG 查詢使用 `skip_scope_check=True`，不受 KB scope 限制。
